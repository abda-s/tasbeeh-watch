.pio
.vscode
# power profiler venv + logs
power_profiler/.venv/
power_profiler/logs/
power_profiler/**/__pycache__/


# desktop simulator runtime state (fake NVS + RTC memory)
.sim_state/

# browser simulator (docs/): LVGL source fetched by build.sh when there's no
# .pio/libdeps to reuse. watch.js/watch.wasm ARE committed (GitHub Pages
# serves them as-is, no build step) -- only this fetched dependency isn't.
docs/.lvgl-cache/
compile_commands.json
